<%= form_for(@translation) do |f| %>
  <% if @translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@translation.errors.count, "error") %> prohibited this translation from being saved:</h2>

      <ul>
      <% @translation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :core_language %><br>
    <%= f.text_field :core_language %>
  </div>
  <div class="field">
    <%= f.label :target_language %><br>
    <%= f.text_field :target_language %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
